using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelectorActionButtons : MonoBehaviour
{
    //public Sprite[] ButtonSprites;
    private ControllerActionButton[] actionButtons;
    // Start is called before the first frame update
    void Start()
    {
        //actionButtons = GetComponentsInChildren<ControllerActionButton>();
        //actionButtons[0].UpdateSprite(ButtonSprites[0]);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
